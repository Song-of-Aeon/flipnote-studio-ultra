global.windowedscale = -1;
function fullscreen_flip() {
	if !window_get_fullscreen() global.windowedscale = global.scale;
	window_set_fullscreen(!window_get_fullscreen());
	if !window_get_fullscreen() && global.windowedscale global.scale = global.windowedscale;
	//global.scale = 
	/*scriptable_create(function() {
		c
		if count > 50 {
			scale = 2;*/
			surfaces_init();
			/*kys;
		}
		
	});*/
	
}