global.affectedpixelstack = [];
global.pixelrecording = false;
global.affectedpixelflush = function() {
	global.affectedpixelstack = [];
}
function pixellog(x, y) {
	if global.pixelrecording array_push(global.affectedpixelstack, new vec2(x, y));
}

function pixelclean() {
	var used = [];
	iterate global.affectedpixelstack to {
		//ITS OVER THIS SUCKS
	}
}